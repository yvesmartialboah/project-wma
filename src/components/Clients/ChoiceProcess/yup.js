import * as yup from 'yup'

export const ChoiceProcessYup = yup.object().shape({
    // numero_search: yup
    // .number()
    // .typeError('Veuillez spécifier un numéro svp.')
    // .required('Le numéro de téléphone est requis !!!'),
   
})